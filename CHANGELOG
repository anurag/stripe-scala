== 1.1.3 2013-05-20

* Fix for issue #13 (`disputed` flag now `dispute`) via markusjura and d6y.
* Added `FeeDetail` to a `Charge`.
* Adjusted `Invoice` classes to match Stripe APIs.
* Upgraded to latest Lift JSON library, defaulting to Scala 2.9.2 and dropping pre-2.9.1 scala.


=== 1.1.2 2012-08-15

* Add new com.stripe.Account API Resource.
* Remove `amount` and `currency` from Token creation tests.
* Use default API bindings key for tests.
